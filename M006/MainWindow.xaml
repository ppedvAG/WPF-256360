<Window x:Class="M006.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:sys="clr-namespace:System;assembly=netstandard"
        xmlns:local="clr-namespace:M006"
        Title="MainWindow" Height="450" Width="800"
		WindowStartupLocation="CenterScreen" Topmost="True"
		DataContext="{Binding RelativeSource={RelativeSource Self}}">
	<Window.Resources>
		<sys:Double x:Key="DefaultFontSize">30</sys:Double>
		<SolidColorBrush x:Key="DefaultBackground" Color="Aqua"/>
		<local:ScoreToGradeConverter x:Key="SGC"/>

		<Style TargetType="Button">
			<Setter Property="Background" Value="{StaticResource DefaultBG}"/>
			<Setter Property="Background">
				Auslagern von Value (für komplexe Properties)
				<Setter.Value>
					<LinearGradientBrush>
						<GradientStop Offset="0" Color="Aqua"/>
						<GradientStop Offset="1" Color="LightGreen"/>
					</LinearGradientBrush>
				</Setter.Value>
			</Setter>
			<Setter Property="Foreground" Value="{StaticResource DefaultFG}"/>
			<Setter Property="FontSize" Value="{StaticResource DefaultFontSize}"/>
		</Style>
	</Window.Resources>
	<StackPanel Margin="10">
		<!--Resources-->
		<!--Objekte/Daten im Frontend ablegen-->
		<!--Wird verwendet für Styles, Templates, Converter, ...-->

		<!--Ressourcen angreifen-->
		<!--StaticResource, DynamicResource-->
		<Button Content="Hallo Welt" FontSize="{StaticResource DefaultFontSize}" Background="{StaticResource DefaultBackground}"/>
		<Button Content="Hallo Welt" FontSize="{DynamicResource DefaultFontSize}"/>
		<Slider Minimum="10" Maximum="50" Value="{Binding Resources[DefaultFontSize]}"/>

		<!--Beispiel: Slider zu Schulnoten umwandeln-->
		<Slider Minimum="0" Maximum="100" Name="ScoreSlider"/>
		<TextBlock Text="{Binding ElementName=ScoreSlider, Path=Value, Converter={StaticResource SGC}}"/>
		
		<!--x:Static-->
		<DatePicker SelectedDate="{x:Static sys:DateTime.Now}"/>
		
		<!--ResourceDictionaries-->
		<!--Ressourcen in einer externen Datei speichern-->
		<!--Siehe Extern.xaml-->
		<Button Content="Test" Background="{StaticResource DefaultBG}" Foreground="{StaticResource DefaultFG}"/>
		
		<!--.............................................................................................................-->
		
		<!--Styles-->
		<!--Allgemeine Sammlung von Eigenschaften für eine Komponente-->
		<!--Alle Eigenschaften können dann auf jede Komponente von diesem Typen angewandt werden-->
		<!--Werden generell als Resource definiert-->
		
		<!--Style direkt im Button Element platzieren-->
		<Button Content="Lokaler Style">
			<Button.Style>
				<!--WICHTIG: Immer TargetType angeben-->
				<Style TargetType="Button">
					<Setter Property="Background" Value="{StaticResource DefaultBG}"/>
					<Setter Property="Foreground" Value="{StaticResource DefaultFG}"/>
					<Setter Property="FontSize" Value="{StaticResource DefaultFontSize}"/>
				</Style>
			</Button.Style>
		</Button>

		<!--Style über eine Resource empfangen (global)-->
		<Button Content="Globaler Style"/>
	</StackPanel>
</Window>
