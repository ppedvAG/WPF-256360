<Window x:Class="M012.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:M012"
        Title="MainWindow" Height="450" Width="800"
		WindowStartupLocation="CenterScreen" Topmost="True"
		DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel>
		<!--Commands-->
		<!--Von der Backendklasse entkoppelte Logik (in eigenen C#-Klassen)-->
		<Button Command="{Binding ExitCommand}"
				Content="Anwendung beenden"/>
		
		<!--CustomCommand-->
		<TextBlock Text="{Binding Counter}" FontSize="30"/>
		<Button Command="{Binding CustomCommand2}"
				Content="Increment"/>

		<!--NuGet: Microsoft.Xaml.Behaviors.Wpf-->
		<!--Namespace: xmlns:b="http://schemas.microsoft.com/xaml/behaviors"-->
		<TextBlock Text="{Binding Output}"/>
		<TextBox Name="TB">
			<b:Interaction.Triggers>
				<b:EventTrigger EventName="KeyDown">
					<b:InvokeCommandAction Command="{Binding EnterCommand}"
										   PassEventArgsToCommand="True"/>
				</b:EventTrigger>
			</b:Interaction.Triggers>
		</TextBox>
	</StackPanel>
</Window>
