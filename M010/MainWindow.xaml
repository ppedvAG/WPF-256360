<Window x:Class="M010.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:M010"
        Title="MainWindow" Height="450" Width="800"
		WindowStartupLocation="CenterScreen" Topmost="True"
		DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel>
		<!--Trigger-->
		<!--Style mit Bedingung-->
		<!--Müssen immer im Style definiert werden-->

		<!--Beispiel: Link wie im Web-->
		<TextBlock Text="Hallo Welt!" FontSize="30">
			<TextBlock.Style>
				<Style TargetType="TextBlock">
					<Style.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Foreground" Value="Blue"/>
							<Setter Property="TextDecorations" Value="Underline"/>
						</Trigger>
					</Style.Triggers>
				</Style>
			</TextBlock.Style>
		</TextBlock>
		
		<!--Beispiel: Wenn eine CheckBox aktiviert ist, soll ein Button angezeigt werden-->
		<CheckBox Content="Hallo" Name="CB"/>
		<Button Content="CheckBox aktiviert">
			<Button.Style>
				<!--Wenn eine Komponente unsichtbar/deaktiviert ist, wird der Style auch deaktiviert-->
				<!--Lösung: Visibility/IsEnabled im Style selbst definieren-->
				<Style TargetType="Button">
					<Setter Property="Visibility" Value="Hidden"/>

					<Style.Triggers>
						<DataTrigger Binding="{Binding ElementName=CB, Path=IsChecked}" Value="True">
							<Setter Property="Visibility" Value="Visible"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Button.Style>
		</Button>
		
		<!--Beispiel: Button aktivieren wenn 4 CheckBoxen aktiviert sind-->
		<CheckBox Content="Hallo" Name="CB1" IsChecked="{Binding CB1Checked, Mode=OneWay}"/>
		<CheckBox Content="Hallo" Name="CB2" IsChecked="{Binding CB2Checked, Mode=OneWay}"/>
		<CheckBox Content="Hallo" Name="CB3" IsChecked="{Binding CB3Checked, Mode=OneWay}"/>
		<CheckBox Content="Hallo" Name="CB4" IsChecked="{Binding CB4Checked, Mode=OneWay}"/>
		<Button Content="CheckBoxen aktiviert">
			<Button.Style>
				<!--Wenn eine Komponente unsichtbar/deaktiviert ist, wird der Style auch deaktiviert-->
				<!--Lösung: Visibility/IsEnabled im Style selbst definieren-->
				<Style TargetType="Button">
					<Setter Property="IsEnabled" Value="False"/>

					<Style.Triggers>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB1, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB2, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB3, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB4, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
					</Style.Triggers>
				</Style>
			</Button.Style>
		</Button>
		
		<!--Beispiel: 2-aus-4-->
		<Button Content="CheckBoxen aktiviert">
			<Button.Style>
				<Style TargetType="Button">
					<Setter Property="IsEnabled" Value="False"/>

					<Style.Triggers>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB1, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB2, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB1, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB3, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB1, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB4, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB2, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB3, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB2, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB4, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
						<MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding ElementName=CB3, Path=IsChecked}" Value="True"/>
								<Condition Binding="{Binding ElementName=CB4, Path=IsChecked}" Value="True"/>
							</MultiDataTrigger.Conditions>
							<Setter Property="IsEnabled" Value="True"/>
						</MultiDataTrigger>
					</Style.Triggers>
				</Style>
			</Button.Style>
		</Button>

		<Button Content="CheckBox aktiviert">
			<Button.Style>
				<!--Wenn eine Komponente unsichtbar/deaktiviert ist, wird der Style auch deaktiviert-->
				<!--Lösung: Visibility/IsEnabled im Style selbst definieren-->
				<Style TargetType="Button">
					<Setter Property="IsEnabled" Value="False"/>

					<Style.Triggers>
						<DataTrigger Binding="{Binding TwoOfFour}" Value="True">
							<Setter Property="IsEnabled" Value="True"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Button.Style>
		</Button>
	</StackPanel>
</Window>
